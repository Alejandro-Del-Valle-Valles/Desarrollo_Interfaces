<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PlantillaElementosExamen.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PlantillaElementosExamen.Views"
    Title="PlantillaElementosExamen">
    <!-- Cambiar el xmlnslocal añadiendo el directorio donde estén las views -->

    <!-- Añadir esto por cada página creada -->
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <!-- TAB BAR
    TabBarBackgroundColor="{StaticResource Primary}"
    TabBarTitleColor="{StaticResource SecondaryDarkText}"
    TabBarUnselectedColor="Black"
    TabBarForegroundColor="{StaticResource SecondaryDark}">
    Esto añadirlo a <Shell

    <TabBar>
        Aquí va el ShellContent
    </TabBar
    -->
</Shell>
